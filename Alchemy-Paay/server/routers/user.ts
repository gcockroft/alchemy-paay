import { Router } from "express";
import { alchemy } from "../handlers/alchemy-sdk"

// Higher level functions 

export function UserRouter(

) {
    const router = Router()
        .get("/", async (req, res) => {

        })
}